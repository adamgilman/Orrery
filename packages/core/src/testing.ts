/**
 * Test doubles, as `@orrery-diagrams/core/testing`: a layout engine that lays out without ELK, so a renderer test
 * never depends on it. Not part of the public API.
 */
export { FakeLayoutEngine } from "./layout.js";
